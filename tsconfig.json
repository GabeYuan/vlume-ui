{
  "compilerOptions": {
    // 保留 JSX 语法,不进行转换
    "jsx": "preserve",

    // 指定要包含的库文件
    "lib": ["esnext", "dom"],

    // 要包含的类型声明文件
    "types": ["node", "unplugin-vue-define-options"],

    // 指定根目录
    "rootDir": ".",

    // 设置基本URL以解析非相对模块名称
    "baseUrl": ".",

    // 指定模块解析策略
    "moduleResolution": "node",

    // 允许导入扩展名为.ts的文件
    "allowImportingTsExtensions": true,

    // 只输出声明文件,不生成 JavaScript 文件
    "emitDeclarationOnly": true,

    // 生成相应的 '.d.ts' 文件
    "declaration": true,

    // 路径映射,简化模块导入
    "paths": {
      "@vlume-ui/*": ["packages/*"]
    }
  }
}
